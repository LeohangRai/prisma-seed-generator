import { PrismaClient } from '@prisma/client';

export const up = async (prisma: PrismaClient) => {
  /* seed logic goes here */
};

export const down = async (prisma: PrismaClient) => {
  /* undo seed logic goes here */
};
